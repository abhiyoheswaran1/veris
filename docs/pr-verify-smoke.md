# PR Verify smoke test

Throwaway file to open a pull request that triggers the `PR Verify` workflow,
so the live `veris verify --github` publishing path (sticky comment + Check Run)
can be confirmed end to end. Safe to delete; this PR is not meant to merge.
